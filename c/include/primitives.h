#ifndef __PRIMITIVES_H__
#define __PRIMITIVES_H__

// Base primtive type
typedef struct Primitive Primitive;

// Create the two kinds of primitives
*Primitive new_point(float x, y, z);
*Primitive new_vector(float x, y, z);



#endif
